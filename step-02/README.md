# Step 2

Simple delivery details form.

Show and hide parts of the page dynamically:

* show the form `deliveryFormVisible` is true, using `ng-show`
* hide the delivery info display when `deliveryFormVisible` is true, using `ng-hide`
* change `deliveryFormVisible` when clicking "Change" and "Hide" using `ng-click`